<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse"
                    data-target="#navbar" aria-expanded="false">
                <span class="sr-only">{{ _("Toggle navigation") }}</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a href="/" class="navbar-header navbar-brand">SPBM</a>
        </div>
        <div id="navbar" class="collapse navbar-collapse">
            <ul class="nav navbar-nav" role="navigation">
                <li{{ active('^/$', attribute=True) }}>
                    <a href="{{ url('overview') }}" class="navbar-link" role="button">
                        <i class="glyphicon glyphicon-dashboard"></i>
                        {{ _("Overview") }}
                    </a>
                </li>
                <li role="separator" class="divider-vertical"></li>
                <li{{ active('workers', attribute=True) }}>
                    <a href="{{ url('workers') }}" title="{{ _("Workers and registrations") }}" class="navbar-link"
                       role="button">
                        <i class="glyphicon glyphicon-user"></i>
                        {{ _("Workers") }}
                    </a>
                </li>
                <li{{ active('events', attribute=True) }}>
                    <a href="{{ url('events') }}" title="{{ _("Events and registrations") }}" class="navbar-link"
                       role="button">
                        <i class="glyphicon glyphicon-calendar"></i>
                        {{ _("Events") }}
                    </a>
                </li>
                <li role="separator" class="divider divider-vertical"></li>
                <li{{ active('invoices', attribute=True) }}>
                    <a href="{{ url('invoices') }}" title="{{ _("Invoicing and payment statuses") }}"
                       class="navbar-link" role="button">
                        <i class="glyphicon glyphicon-transfer"></i>
                        {{ _("Invoicing") }}
                    </a>
                </li>
                <li{{ active('wages', attribute=True) }}>
                    <a href="{{ url('wages') }}" title="{{ _("Wage checklist and wage system export") }}"
                       class="navbar-link" role="button">
                        <i class="glyphicon glyphicon-piggy-bank"></i>
                        {{ _("Wages") }}
                    </a>
                </li>
            </ul>
            <ul class="nav navbar-nav navbar-right" role="navigation">
                {% if user.is_staff -%}
                    <li>
                        <a href="{{ url('admin:index') }}" title="{{ _("Go to administration panel") }}">
                            <i class="glyphicon glyphicon-cog"></i>
                            <span class="hidden-md hidden-sm">{{ _("Administration") }} </span>
                        </a>
                    </li>
                {%- endif %}
                <li>
                    <a href="{{ url('logout') }}" class="navbar-link" title="{{ _("Log out of SPFs SPBM") }}">
                        <i class="glyphicon glyphicon-log-out"></i>
                        <span class="hidden-md hidden-sm">{{ _("Logout") }} </span>
                    </a>
                </li>
            </ul>
            <p class="navbar-text navbar-right hidden-sm">
                <span title="{{ user.spfuser }}">
                    <small>{{ user.spfuser.society.shortname }}</small>
                    {{ user }}
                </span>
            </p>
        </div>
    </div>
</nav>
