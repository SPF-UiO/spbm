{% extends "base.jinja" %}

{% block content %}
<div>
<table class="table table-striped">
<tr><th>Number</th><th>Society - period</th><th>Cost</th><th>Paid</th></tr>
{% for invoice in invoices %}
<tr><td>{{ invoice.invoice_number }}</a></td><td><a href="/invoices/{{ invoice.society.shortname }}/{{invoice.period}}.pdf">{{ invoice.society.shortname }} - {{ invoice.period }}</a></td><td>{{ invoice.get_total_cost() }}</td><td>{{ invoice.paid }}</td></tr>
{% endfor %}
</table>
</div>
{% endblock %}
